import { createMuiTheme } from '@material-ui/core'

const theme = createMuiTheme({
    palette: {
        type: 'light',
        primary: {
            light: '#424242',
            main: '#424242',
            dark: '#424242',
        }
    }
})

export default theme